Product photos for the site. Any .jpg/.png/.webp dropped here is auto-rendered
by the gallery strip in app/page.tsx (build-time scan). Guidelines: ~1600px wide,
<1 MB each ideally (hard Workers limit 25 MiB/file); filenames become alt text
(e.g. pastel-faux-fur-scarf.jpg). Sources: Figma pull (scripts/figma-pull.mjs with
FIGMA_TOKEN) or drop exported originals directly.
